"""Channel variants — per-variant rule tables.

Each variant module exposes a ``diagnose(...)`` function that the
``channel`` collector dispatches to after detection. The base module
defines the shared finding shape.
"""
