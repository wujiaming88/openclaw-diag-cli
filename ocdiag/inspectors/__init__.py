"""Object inspector package — inspectors take a session uuid and inspect it.

Members:
  trace      — single user-message timeline + analysis
  extract    — session record export
  panorama   — full 360° view (correlation graph + every standard source)
"""
